{
  // 1.
  const hello: string = "Hello";
  console.log(hello);
}
{
  // 2.
  const num: number = 42;
  console.log(num);
}
{
  // 3.
  const bool: boolean = true;
  console.log(bool);
}
{
  // 4.
  const numArr: Array<number> = [1, 2, 3];
  console.log(numArr);
}
{
  // 5.
  const strArr: string[] = ["a", "b", "c"];
  console.log(strArr);
}
{
  // 6.
  const tuple: [number, string] = [3, "a"];
  console.log(tuple);
}
{
  // 7.
  const nul: null = null;
  console.log(nul);
}
{
  // 8.
  const unde: undefined = undefined;
  console.log(unde);
}
{
  // 9.
  const obj: { name: string; age: number } = { name: "jeong", age: 24 };
  console.log(obj);
}
